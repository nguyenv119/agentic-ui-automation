export const initDataset = () => {
  return "";
};

export const writeWorkflowJSON = (
  dir: string,
  {
    agentATask,
    agentBPlan,
    steps,
  }: { agentATask: string; agentBPlan: any; steps: any }
) => {
  return "";
};
